$('#actions').append("<%= escape_javascript render('actions', :actions => @actions) %>");
$('#pagination').replaceWith("<%= escape_javascript(link_to_next_page(@actions, 'Load More', :remote => true, :class => 'btn', :id => 'pagination', :data => { 'loading-text' => 'Loading...' })) %>");